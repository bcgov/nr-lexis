name: PR Closed

on:
  pull_request:
    types: [closed]

concurrency:
  # PR open and close use the same group, allowing only one to mutate a
  # preview environment at a time.
  group: ${{ github.event.number }}
  cancel-in-progress: true

permissions: {}

jobs:
  cleanup:
    name: Cleanup and Image Promotion
    uses: bcgov/quickstart-openshift-helpers/.github/workflows/.pr-close.yml@ca6579bdadf383a2513397081f963889b92823a4 # v1.1.0
    permissions:
      packages: write
    secrets:
      oc_namespace: ${{ secrets.oc_namespace }}
      oc_token: ${{ secrets.oc_token }}
    with:
      cleanup: label
      packages: backend frontend
      cleanup_name: nr-lexis-backend-${{ github.event.number }} nr-lexis-frontend-${{ github.event.number }}

  cleanup-networkpolicies:
    name: Cleanup PR NetworkPolicies
    needs: cleanup
    runs-on: ubuntu-slim
    timeout-minutes: 5
    steps:
      - name: Delete per-PR NetworkPolicies
        uses: bcgov/action-oc-runner@d2fc921aaa9d70684c186dbb7754e6985d44d86f # v1.4.1
        with:
          oc_namespace: ${{ secrets.oc_namespace }}
          oc_token: ${{ secrets.oc_token }}
          oc_server: ${{ vars.oc_server }}
          commands: |
            set -eu
            for label in \
              "nr-lexis-backend-${{ github.event.number }}" \
              "nr-lexis-frontend-${{ github.event.number }}"
            do
              echo "Deleting networkpolicies matching app=${label}"
              oc delete networkpolicy -l "app=${label}" --ignore-not-found
            done
