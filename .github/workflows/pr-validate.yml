name: PR Validate

on:
  pull_request:
    types: [edited, opened, synchronize, reopened, ready_for_review]

concurrency:
  group: ${{ github.workflow }}-edit-${{ github.event.number }}
  cancel-in-progress: true

permissions: {}

jobs:
  vars:
    name: Variables
    runs-on: ubuntu-24.04
    outputs:
      slot: ${{ steps.calc.outputs.slot }}
    steps:
      - id: calc
        run: |
          slot=$(( ${{ github.event.number }} % 50 ))
          echo "slot=${slot}" >> "$GITHUB_OUTPUT"

  validate:
    name: Validate PR
    if: >-
      !github.event.pull_request.draft &&
      github.event.pull_request.head.repo.full_name == github.repository
    needs: [vars]
    permissions:
      pull-requests: write
    uses: bcgov/quickstart-openshift-helpers/.github/workflows/.pr-validate.yml@ca6579bdadf383a2513397081f963889b92823a4 # v1.1.0
    with:
      markdown_links: |
        - [Frontend](https://${{ github.event.repository.name }}-${{ needs.vars.outputs.slot }}.apps.gold.devops.gov.bc.ca)
        - [Backend](https://${{ github.event.repository.name }}-${{ needs.vars.outputs.slot }}.apps.gold.devops.gov.bc.ca/api)

  results:
    name: Validate Results
    if: always()
    needs: [validate]
    runs-on: ubuntu-slim
    steps:
      - if: contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')
        run: echo "PR validation failed or was cancelled." && exit 1
      - run: echo "Success!"
