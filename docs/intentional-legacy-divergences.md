# Intentional legacy divergences

Modern LEXIS normally preserves functional parity with legacy. Intentional modernizations, security
controls, and approved retirements use the searchable code marker
`INTENTIONAL_LEGACY_DIVERGENCE(<ID>)` and are recorded here so they are not mistaken for parity
defects.

Only observable differences in supported inputs, user workflow, authorization, persistence, or
integration behaviour belong in the divergence table. Refactors, duplicated service-boundary
enforcement, and early validation of existing legacy or database constraints are parity work.
Stored-procedure replacements are listed separately below as an implementation inventory.

The business-approved 100-row default applies only to IDIR Application Review, which also offers
150/200-row options. All other search pages retain the legacy 10-row default; the general Search
Results wording in Confluence does not expand the 100-row default to those pages.

| ID                                      | Modern behaviour                                                                                                                                                                                                             | Reason                                                                                                                        |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `FEDERAL_INTERIOR_HARVESTED_WITHOUT_PACKAGE` | Federal Interior harvested applications without a summary of scale can be imported without a boom/package number. Both XML validation and validation before persistence permit an application without package/scale rows for RCB, RKB, RNO, ROM, RTO and RSK. Coastal, provincial, standing, supplied-boom and re-advertisement rules remain unchanged. | Business direction confirmed September 23, 2026 to remove the legacy rejection and manual transcription into the Advertising List. |
| `SEARCH_STATE_PERSISTENCE`              | Applied search filters, sorting, and paging survive in-app navigation. Logout, session expiry, login, and organization changes clear the saved state. A fresh authenticated visit shows no results until Search is selected. | UI review request; legacy does not retain search state across page navigation.                                                |
| `SEARCHABLE_CLIENT_SELECTION`          | Editable client fields across active search, report, and create/edit workflows offer client-name prefix or exact client-number lookup with name/number suggestions. Selecting or replacing a client submits its canonical client number; authorization, required client locations, and fixed-identity restrictions remain unchanged. Acronym matching is not included in this name/number feature. | Client discovery improvement aligned with FSPTS Agreement holder lookup; legacy requires client-number entry without name suggestions. |
| `APPLICATION_REVIEW_PAGE_SIZE`          | IDIR Application Review defaults to 100 results per page. Ordinary searches retain the legacy default of 10. | Business-confirmed scope for the larger review queue; the 100-row default does not apply to other search pages. |
| `CLEAR_ALL_RESETS_SEARCH`               | Clear all resets the filters and returns table-backed search pages to their initial state with no results table. It does not submit an unfiltered search. | Business and UI review direction to avoid distracting users with results that no longer correspond to visible filter selections. |
| `CONTEXTUAL_RETURN_NAVIGATION`           | A detail page one level below its parent exposes one deterministic named Back link; deeper detail navigation exposes a Carbon breadcrumb of accessible ancestors. Search, review, summary, and nested-detail navigation record the actual originating target and preserve its exact URL and router state; direct or bookmarked detail visits use the canonical accessible parent or role default. | Heartwood navigation requirement; legacy exposes display-only section breadcrumbs and fixed search or cancel targets, with no originating-list or query-state contract. |
| `RESPONSIVE_SIDE_NAVIGATION`             | Side navigation remains available at narrow widths as a collapsed icon rail that can be temporarily expanded. Its narrow-screen state is independent of the persisted desktop collapsed or expanded preference. | Heartwood navigation requirement; legacy exposes a fixed-width text menu with no responsive collapsed state or saved navigation preference. |
| `NAVIGATION_MENU_CONTRACT`               | Search links are labelled Application search, Exemption search, Offer search, and Permit search; Federal uses Application search; Admin navigation and page headings use Multiplication Factor and Non-appraised Sec.3 FIL%. Only the group containing the current page starts open. Collapsing the panel shows one icon per group; selecting a collapsed group opens the panel with only that group open. The header toggle restores previously open groups, and selecting a destination preserves group expansion. The current marker moves from the child page to its closed group. Provincial Submitters do not receive Federal, Reports, or Admin sections. | Business-approved menu terminology and role visibility, with group behaviour following the [Heartwood side navigation standard](https://apps.nrs.gov.bc.ca/int/confluence/display/FSAST1/Side+navigation); regression passes must not restore legacy labels or superseded expansion behaviour. |
| `DETAIL_VIEW_EDIT_MODES`                | Authorized detail pages open in view mode and expose explicit Edit and Cancel actions.                                                                                                                                       | UI review request to follow the modern design pattern instead of legacy's always-editable presentation.                       |
| `BCEID_APPLICATION_EDIT_CONTRACT`       | Authorized BCeID application edits remain available through the modern edit flow without depending on optional legacy-only status or remarks fields. A legacy client-side null dereference for those omitted fields is not reproduced.                                                                 | Preserve the authorized application-edit capability while treating the legacy missing-field failure as a defect, not as a modern functional contract.                                      |
| `APPLICATION_LIST_DATE_SELECTION`       | On the Provincial Application tab, Application Approvers no longer see or edit Date received. List date uses radio choices instead of a dropdown; the legacy Blank option is labelled No list date. On a list date, Approvers also see that day's date alongside the next two dates. Clients cannot change List date once the application is Approved. | Business-confirmed Application tab enhancement. Legacy exposes Date received to staff, uses a dropdown with Blank, and does not offer the same-day date in its next-schedule choices. Legacy permissions let clients edit Approved applications before the list date, but its client save fails, so clients never changed an approved List date in practice. |
| `STRICT_DATE_INPUT_VALIDATION`          | Manually entered dates must be real calendar dates in the supported format. Invalid text remains visible for correction and is rejected before persistence instead of being cleared or normalized to a different date.                                                                                   | Bug fix. Legacy date handlers state that invalid calendar dates should be rejected, but their pattern checks can admit impossible dates and lenient `SimpleDateFormat` parsing can persist a different date than the user entered. |
| `PACKAGE_FIRST_ITEMS_WORKFLOW`          | Modern package-backed Items workflows hide dependent Package Details and Summary of Scale content until a package exists. Where creation is authorized, package creation is the primary action; read-only views retain only the package empty state. Unsaved application create explains and enforces the save-first prerequisite. Existing-package views use the business term Summary of Scale and retain multi-package support. | Business-confirmed modernization. A package is required before Summary of Scale entry, package creation is primary for the common one-package surplus-test application, and Summary of Scale is the correct term for scaler or third-party summary data. Legacy instead renders empty dependent sections before the prerequisite action. |
| `BOIC_PACKAGE_STATUS_DEFAULTS` | Blanket OIC package Status and Reprocessed are hidden from all users in Create, saved details, and Edit. New packages save Active and No; edits preserve the existing stored values. | Business direction: these fields no longer affect the export process. Historical values remain unchanged when other package information is edited. |
| `EXEMPTION_CREATE_SAVE_FIRST` | Exemption creation uses the conditional legacy tabs while keeping the draft unsaved until Save. Owner data is derived from linked applications; Documents and Permits become available after creation. | Dependent actions require a persisted exemption number. The draft tabs explain this requirement without creating an incomplete exemption just to display the legacy workflow. |
| `MINISTERIAL_PERMIT_EMPTY_CREATION` | Creating a Ministerial permit persists an active, numbered permit with no applications or scales attached. Users choose eligible applications explicitly afterwards. Ordinary OIC permits retain automatic scale attachment. | The checked Ministerial creation prototype starts with empty Scale and Fees tabs and unchecked application choices. |
| `MINISTERIAL_SCALE_SELECTION_EDIT` | Ministerial scale membership starts read-only. Edit scale selection creates a local draft; Cancel discards it and Save changes persists the changed rows together in one transaction. Package identity, scale eligibility, role/status locks and application reconciliation remain authoritative on the server. | Approved Ministerial Scale view/edit prototype (2513:18112 and 2515:25858); prevents checkbox clicks from persisting before explicit save. |
| `MINISTERIAL_PERMIT_CLIENT_LOCATIONS` | Ministerial permit applicant and agent numbers remain bound to the exemption; authorized edits can change their validated client locations without changing client defaults. | UX review PER-6 allows a different invoice address while keeping the permit tied to the exemption's clients. |
| `ADMIN_PAGE_RETIREMENT`                 | Users & Access, IDIR lookup, Agent Status, Log Export WSDL, and BiWeekly Listing WSDL administration are not exposed. Active administration remains available for Multiplication Factor, Non-appraised Sec.3 FIL%, and Average market values.                                                            | Business-requested retirement. Agent processing is being sunset, LEXIS now provides the gateway function directly, and the legacy web-service administration is being retired. |
| `ADMINISTRATOR_SUPER_ROLE`              | Modern Administrator can perform every active modern LEXIS function, including Ministerial exemption approval, Approved Exemption printing, and application-submission import. Retired report screens remain denied as documented in `RETIRED_REPORT_SCREENS`. | Modern Administrator is intentionally the all-access role for active functions.                                                |
| `MULTI_FILE_UPLOAD_QUEUE`               | Application submission and document workflows can queue, validate, review, and submit multiple files. Each document file has its own optional description. Invoice uploads retain one file per invoice.                                                                           | Modern workflow improvement; legacy upload pop-ups accepted one `formFile`. Descriptions remain stored per file, and the shared database creates one invoice per invoice upload. |
| `SEARCHABLE_UPLOAD_TARGET_WORKFLOW`     | Modern consolidated upload pages let authorized users search for and select an exemption or permit using descriptive labels. Legacy launches a record-bound upload pop-up and carries the numeric target from the current record. Both workflows persist only the numeric identifier. | Modern consolidated upload workflow; canonical numeric persistence remains legacy-compatible. |
| `INVOICE_NUMBER_ENCODING_VALIDATION`    | Invoice creation and uploads reject control and non-US-ASCII invoice identifiers before persistence. Required, nine-character, positive-value, and Oracle numeric-fit checks preserve legacy and database behaviour. | Bug fix. Legacy validates invoice-number presence and length but can pass identifiers that the `US7ASCII` `VARCHAR2(9 BYTE)` columns cannot represent. |
| `PERMIT_OVERRIDE_STORAGE_POSITIVITY`    | Enabled permit fee overrides must remain positive after Oracle's two-decimal storage rounding; values that round to `0.00` are rejected before persistence. | Bug fix. Legacy accepts smaller positive values that Oracle stores as `0.00`, after which the saved permit immediately presents the override as disabled. |
| `RTM_AMV_SPREADSHEET_UPLOAD`            | Administrators maintain AMV values through the XLSX validation, review, and atomic upload workflow. The former editable-grid route redirects to that workbook workflow, which retains the existing schema and synchronization trigger. | Provides the approved workbook-only AMV experience while preserving the authoritative AMV data contract.                       |
| `CLAMAV_UPLOAD_SCANNING`                | Deployed uploads are scanned through the shared ClamAV service before persistence. Infected files and unavailable scans are rejected.                                                                                        | Modern security control; legacy upload actions did not perform malware scanning.                                              |
| `INDIGENOUS_RESERVE_MODULE_RETIREMENT`  | Legacy Indian/Indigenous Reserve search, create, detail, API, authorization-action, and report-jurisdiction surfaces are not exposed.                                                                                        | Intentional retirement of the Indigenous Reserve module.                                                                      |
| `ADVERTISING_REPORT_VARIANT_RETIREMENT` | The `generateIndustryPDF` and `generateIndustryCSV` shortcuts and legacy root `biweeklyListing` routes are retired. The filtered Advertising List remains and exposes PDF/CSV as an output-format choice.                    | Consolidates duplicate legacy shortcuts into one maintained report workflow.                                                  |
| `RETIRED_REPORT_SCREENS`                | Modern LEXIS excludes Application Report, Create TEAC Package, Exemptions Report, and Fees Report from navigation, redirects their UI routes, and denies their modern and legacy-compatible API routes. Legacy action metadata may retain those action keys for session compatibility, but the grants do not authorize the retired routes. Authorized users may still access the historical screens in the separate legacy application. | Business-confirmed retirement. |
| `NOTIFICATION_MODULE_ADDITION`          | Authenticated users can read active, role-targeted operational notifications. Authorized administrators can manage the notification schedule and content.                                                                    | Adds an in-application operations channel that does not exist in legacy.                                                      |
| `SEARCH_FILTER_EXPANSION`               | Provincial application and exemption searches add received/approval date ranges, and permit search visibly exposes the invoice-number criterion.                                                                             | Exposes useful criteria supported by the modern query contract; the legacy screens keep them hidden.                         |
| `HISTORICAL_ORG_UNIT_SEARCH_VISIBILITY` | An exact modern exemption search can return a historical or inactive organizational-unit record, such as exemption `12-8238`, when the record is otherwise authorized and directly identified.                                                                                              | Preserve exact-record discoverability for historical data; legacy's active-region default can omit records belonging to an older organizational unit and is not an intentional modern search constraint. |
| `FEDERAL_READ_ONLY_BCEID` | `LEXIS_FEDERAL_READ_ONLY` lets NEXCOL Business BCeID users search and view all federal applications without a forest client. No other module or write access is granted. | Business direction; separate from the broader IDIR Read Only role. |
| `FAM_STAFF_GLOBAL_DATA_SCOPE`           | IDIR Administrators, Application Approvers, Exemption Approvers, and Read Only users granted a role without a region have province-wide record scope within the actions granted to their roles. The same Approver or Read Only role granted through its FAM `_REGION` role reaches only the chosen Natural Resource Regions' records, per role and action; Administrators are always province-wide. Zone and region selections remain defaults and filters, not authorization boundaries. Provincial Submitter identities remain separately scoped to records associated with their selected FAM forest client. | Replaces legacy WebADE selected-organization regional scoping with the approved FAM role-and-client authorization model, with explicit region-scoped FAM roles where regional access is required. LEXIS enforces staff-only IDIR roles and client-scoped Provincial Submitter and unscoped Federal Read Only BCeID Business roles during interactive SSO token conversion; FAM should also reject incompatible assignments at provisioning time. |
| `READ_ONLY_STAFF_VIEW`                  | Pure Read Only has the Application Approver baseline for supported business-record searches, details, remarks, documents, and retained reports. It cannot create, edit, approve, upload, send workflow email, or access review queues; the Approved Exemption report remains outside that shared baseline. | Business direction. Legacy Read Only was unfinished, so its incomplete workflow is not the modern authorization baseline. |
| `FEDERAL_DETAIL_APPROVAL_TRANSITION`    | Authorized federal detail users can approve a `NEW` or `PND` application from the detail page. The backend applies the shared Application Review readiness validation before changing the status to `APP`.                  | Keeps the preferred detail-page action without bypassing the legacy approval domain gate.                                     |
| `ASYNC_WORKFLOW_EMAIL_DELIVERY`         | Workflow mutations complete when their business transaction commits and queue any resulting email for best-effort asynchronous delivery after commit. A successful workflow response confirms the mutation, not SMTP delivery; delivery failures are logged and metered. | Prevents SMTP latency or failure from rolling back or delaying an already committed business operation.                       |
| `CANONICAL_SEARCH_RESULTS`              | Provincial exemption and permit searches return one row per exemption or permit, and counts use the same canonical candidate set. Joined application, region, and scale rows do not duplicate results or multiply balances. | Avoids legacy join fan-out that can duplicate exemptions, inflate permit counts, and multiply permit-volume balance values.    |
| `CANONICAL_EXEMPTION_DETAILS` | Exemption detail and the Approved Exemption report return one row per exemption, retaining exemptions without applications. Client/date context uses the search ordering: latest advertising date (nulls last), then highest application number. Remaining volume counts assigned scales once across all linked applications; regions contain descriptive names. Permit balance reads and Interior Ministerial item-edit eligibility use the corrected remaining volume; Ministerial permit client validation uses the selected client context. | Fixes legacy region-join multiplication, numeric region labels and arbitrary client/date selection or repeated report sections. Scale eligibility, permit-status treatment, null/negative balance handling and role/client-access checks remain unchanged; this is a deliberate defect correction, not exact reproduction of the procedure output. |
| `PERMIT_APPROVAL_EMAIL_RESEND`          | Eligible permit reviewers expose an Email approval action that can preview and resend the approval message.                                                                                                                  | Provides an operational recovery action after the original permit approval workflow without granting approval-email capability to Provincial Submitters.   |
| `EXPIRED_DOCUMENT_MAINTENANCE`          | Authorized Application Approvers, Provincial Submitters, and Administrators can add and delete direct application and permit documents after the record expires. All other expired-record changes remain locked, and invoice documents remain active-permit-only.                           | Business-requested exception so reconciliation documents received after expiry can still be maintained.                       |
| `SAFE_EXEMPTION_INITIAL_STATUS`         | Ministerial exemptions are created as `NEW`; OIC and Blanket OIC exemptions are created as `ACT`. The API rejects a mismatched initial status before persistence.                                                            | Prevents invalid or partially activated exemptions that legacy's user-selectable initial status allowed.                      |
| `AUTHORITATIVE_CLIENT_LOCATION_LOOKUP`  | Client contact details are loaded using the client and persisted location code together.                                                                                                                                     | Avoids a legacy display defect that can show contact data from a different/default client location.                           |
| `COMPLETE_SHIPPING_COUNTRY_OPTIONS`     | Federal and permit shipping details expose and resolve every active country reference code, including persisted values outside legacy's initial short list.                                                                  | Avoids a legacy display defect where a valid country such as `AD` renders blank after the short-list selector is initialized. |
| `PROVINCIAL_SUMMARY_CLIENT_SCOPE`       | Provincial Summary remains a client-scoped BCeID dashboard for Provincial Submitters. My Exemptions includes ordinary Ministerial and OIC exemptions associated with the selected FAM forest client; non-client-specific Blanket OIC exemptions remain globally visible. Results are canonical, with one row per exemption. Summary is not exposed to Read Only staff, who view the underlying records through the provincial search tables. | Legacy Read Only was unfinished and its clientless Summary is not authoritative for staff access. Legacy BCeID Summary can also omit client-linked OIC and Blanket OIC records that its Exemption Search can return; modern preserves selected-client ownership for ordinary exemptions and the established global scope for Blanket OIC. |

The Advertising List change retired two legacy shortcuts, not two complete reports. The Approved
Exemption and Permit printable reports remain available from their respective detail pages.

Business direction confirms the retirement of Application Report, Create TEAC Package, Exemptions
Report, and Fees Report.

Keep markers next to the controlling code rather than on every consuming component.

## Stored-procedure replacements

These active modern paths replace legacy procedure calls. This inventory describes application
usage; the shared database procedures remain available to legacy and other callers.

| Modern path | Legacy procedure calls replaced | Current implementation |
| --- | --- | --- |
| Attachment type list and descriptions | `LEXIS_CODES.FIND_ALL_ATTACH_CODES`, `LEXIS_CODES.FIND_ATTACH_TYPE_CODE` | Direct SELECTs in [LexisCodeQueries](../backend/src/main/java/ca/bc/gov/mof/lexis/repository/reference/LexisCodeQueries.java), preserving historical codes. |
| Shipping transport options | `LEXIS_CODES.FIND_ALL_TRANSPORT_TYPE_CODES` | Direct SELECT with the existing validity filter, ordering-table join and ordering. Individual transport-code validation is listed below. |
| Shipping/report port options and permit port validation | `LEXIS_CODES.FIND_ALL_PORT_CODES`, `LEXIS_CODES.FIND_PORT_CODE` | Direct SELECTs in `LexisCodeQueries`; lists retain the validity filter, while by-code validation retains the legacy equality predicate and accepts historical codes. |
| Application/report growth-type options | `LEXIS_CODES.FIND_ALL_GROWTH_TYPE_CODES` | Direct SELECT in `LexisCodeQueries`, preserving the seven-column projection, validity filter, ordering-table join and ordering. Reports still prepend All; individual growth-code lookups still use `FIND_GROWTH_TYPE_CODE`. |
| Application/application-review product-type options | `LEXIS_CODES.FIND_ALL_PRODUCT_TYPE_CODES` | Direct SELECT in `LexisCodeQueries`, preserving the seven-column projection, validity filter, ordering-table join and ordering. Both callers still prepend All; individual product-code lookups still use `FIND_PRODUCT_TYPE_CODE`. |
| Shipping/permit/report country lists | `LEXIS_CODES.FIND_ALL_COUNTRY_CODES`, `LEXIS_CODES.FIND_COUNTRY_GROUP` | Direct SELECTs in `LexisCodeQueries`, preserving the seven-column projection, validity filter, ordering-table join and caller-specific mapping/validation. Permit sorting retains group/order metadata; grouped report options retain the group bind and order by ordering-table position. |
| Application/federal/review status options | `LEXIS_CODES.FIND_ALL_APP_STATUS_CODES` | Direct SELECT preserves the seven-column projection, validity and ordering; each caller retains its status filters and All behavior. |
| Application/federal/exemption/report type, reason and status options | `LEXIS_CODES.FIND_ALL_EXEMPTION_TYPE_CODES`, `LEXIS_CODES.FIND_ALL_EXEMPT_RSN_CODES`, `LEXIS_CODES.FIND_ALL_EXEMPT_STS_CODES` | Direct SELECTs preserve validity, joins and ordering, with existing caller-specific filtering, null handling and All placement. |
| Report jurisdiction and permit/report status options | `LEXIS_CODES.FIND_ALL_JURISDICTION_CODES`, `LEXIS_CODES.FIND_ALL_PERMIT_STATUS_CODES` | Direct SELECTs retain the legacy projection, validity and ordering. Report jurisdiction filtering and All behavior are unchanged. |
| Application species and package-status options | `LEXIS_CODES.FIND_ALL_SPECIES_CODES`, `LEXIS_CODES.FIND_ALL_PACKAGE_STATUS_CODES` | Direct SELECTs retain all seven columns, validity, joins and ordering, required named-column mapping, zero metadata defaults, invalid-row filtering and stable group/order sorting. |
| Region options, org-unit validation/display and report default-region lookup | `LEXIS_CODES.FIND_ORG_UNIT_BY_NUMBER`, `LEXIS_CODES.FIND_ORG_UNIT_BY_CODE` | Direct SELECTs preserve all 15 org-unit columns and exact key predicates, including historical org units. Region options retain eight ordered String binds; validation/display/default-region callers retain their input guards, first-row behavior and strict or optional mapping. |
| Application and exemption code validation | `LEXIS_CODES.FIND_APPLICATION_STATUS_CODE`, `LEXIS_CODES.FIND_EXEMPTION_REASON_CODE`, `LEXIS_CODES.FIND_APPLICANT_TYPE_CODE`, `LEXIS_CODES.FIND_JURISDICTION_CODE`, `LEXIS_CODES.FIND_EXEMPTION_TYPE_CODE`, `LEXIS_CODES.FIND_EXEMPTION_STATUS_CODE` | Bound direct SELECTs retain all seven columns, active-date joins, exact code equality and legacy ordering where defined. Validators still compare the first trimmed code, including null/empty handling and propagated query/column errors. |
| Permit and federal country/transport validation, permit status/scale-method validation | `LEXIS_CODES.FIND_COUNTRY_CODE`, `LEXIS_CODES.FIND_TRANSPORT_TYPE_CODE`, `LEXIS_CODES.FIND_PERMIT_STATUS_CODE`, `LEXIS_CODES.FIND_SCALE_METHOD_CODE` | Bound direct SELECTs retain the active-date code/ordering-table join and equality predicate. Permit validation still tests row existence; federal validation still accepts any matching named CODE value. |
| Upload file-type validation | `LEXIS_CODES.FIND_FILE_TYPE_CODE` | Bound direct SELECT preserves historical codes without a date filter or ordering-table join, required named-column mapping and first-row comparison. Upload persistence procedures remain unchanged. |
| Provincial/federal application searches | `LEXIS_GROUP_5.FIND_APPLICATIONS_BY_CRITERIA`, `LEXIS_GROUP_5.COUNT_APPLICATIONS_BY_CRITERIA` | Direct count/page queries in [LexisApplicationRepository](../backend/src/main/java/ca/bc/gov/mof/lexis/repository/application/LexisApplicationRepository.java) and [FederalApplicationRepository](../backend/src/main/java/ca/bc/gov/mof/lexis/repository/federal/FederalApplicationRepository.java). |
| Exemption search | `LEXIS_GROUP_5.FIND_EXEMPTIONS_BY_CRITERIA`, `LEXIS_GROUP_5.COUNT_EXEMPTIONS_BY_CRITERIA` | Direct count/page queries in [ExemptionRepository](../backend/src/main/java/ca/bc/gov/mof/lexis/repository/exemption/ExemptionRepository.java). |
| Exemption detail, application/permit consumers and Approved Exemption report | `LEXIS_GROUP_5.FIND_EXEMPTION_BY_NUMBER` | Shared bound [ExemptionDetailQueries](../backend/src/main/java/ca/bc/gov/mof/lexis/repository/exemption/ExemptionDetailQueries.java) preserves the 17-column projection and applies `CANONICAL_EXEMPTION_DETAILS` above. Other procedure consumers remain unchanged; no schema/data migration. Validated September 21, 2026 with 115 local contract/report tests, 13 synthetic read-only Oracle cases and a schema-qualified access check; deployed multi-application PDF and named-role acceptance remain separate release checks. |
| Provincial permit search | `LEXIS_GROUP_5.FIND_PERMIT_BY_CRITERIA`, `LEXIS_GROUP_5.COUNT_PERMIT_BY_CRITERIA` | Direct count/page queries in [PermitRepository](../backend/src/main/java/ca/bc/gov/mof/lexis/repository/permit/PermitRepository.java). |
| Exemption/permit document lists | `LEXIS_GROUP_5.FIND_EXEMPT_FILE_DETAILS`, `LEXIS_GROUP_5.FIND_PERMIT_FILE_DETAILS` | Direct context queries in [ExemptionDetailsRpcRepository](../backend/src/main/java/ca/bc/gov/mof/lexis/repository/exemption/ExemptionDetailsRpcRepository.java) and [PermitRpcRepository](../backend/src/main/java/ca/bc/gov/mof/lexis/repository/permit/PermitRpcRepository.java) combine direct/linked documents, type descriptions and ownership. The old repository methods remain but are not used by these service paths. |
| AMV workbook saves | `RTM_EMS_LOG_AMV_INSERT`, `RTM_EMS_LOG_AMV_UPDATE` | Transactional `MERGE` for reviewed batches and compatibility workbook uploads; see the [RTM procedure boundary](rtm-amv-ui-and-procedure-contract.md#legacy-procedure-boundary). Legacy single-row methods remain in source but have no exposed mutation route. |

Other procedures still have modern callers: application documents use `LEXIS_GROUP_5.FIND_APPL_FILE_DETAILS`,
and package/scale/end-use procedures remain in use outside the consolidated permit detail queries.
Advertising List still calls `LEXIS_REPORTING.BIWEEKLY_REPORT_CSV` for CSV and
`LEXIS_REPORTING.BIWEEKLY_RPT` / `BIWEEKLY_SUBREPORT_RPT` for PDF; these are not retired.
