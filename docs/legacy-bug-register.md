# Legacy bug register

## Corrections implemented

| ID | Finding | Correction and evidence |
| --- | --- | --- |
| LEGACY-001 | Legacy package update dereferences a missing exemption type in `ApplicationDetailsRPCAction.updatePackage`. | **Already handled in modern:** valid package updates do not require a linked exemption. If legacy itself needs a patch, compare the exemption type null-safely before applying exemption-specific rules. The source defect is confirmed; whether it caused the historical legacy TEST error remains unconfirmed without logs. |
| LEGACY-002 | Legacy uses dropdown display text as the package key, losing trailing spaces and potentially targeting the plain sibling. | **Implemented** through [PR #228](https://github.com/bcgov/nr-lexis/pull/228): preserve exact stored keys. TEST edit, capacity, scale, fee and deletion checks passed. Verification still to record: eligible offer save/readback and ordinary permit-availability isolation for plain/padded siblings. |
| LEGACY-003 | A legacy-created package can lack age class, causing modern validation to block even a comment edit. | **Implemented** by [edb1d73b](https://github.com/bcgov/nr-lexis/commit/edb1d73b) ([PR #233](https://github.com/bcgov/nr-lexis/pull/233)): use the saved application age when package age is missing, OIC is explicitly No, and package product is missing or matches the application. Existing age and BOIC behavior are preserved; regression tests cover the fallback and exclusions. **DEV-33 acceptance passed, 14 September 2026:** an affected ordinary padded package saved without the age-class error; fresh modern readback and independent legacy reads confirmed the changed comment, unchanged plain sibling and restored original comment. |
| LEGACY-004 | Timber-mark joins duplicated scale rows, totals and fee inputs. | Join the unique timber-mark row. [Modern PR #214](https://github.com/bcgov/nr-lexis/pull/214), [DB PR #735](https://github.com/bcgov-c/nr-mof-db/pull/735). **Resolved:** business confirmed correct PROD display after the 3 September 2026 deployment. |
| LEGACY-005 | Legacy displayed location `00` contacts instead of the saved client location. | Use client plus saved location. [Recorded correction dd2029fb](https://github.com/bcgov/nr-lexis/commit/dd2029fb); historical TEST comparison passed. |
| LEGACY-006 | Valid shipping countries outside legacy's short list displayed blank. | Load all active country codes. [e774917d](https://github.com/bcgov/nr-lexis/commit/e774917d); [Andorra regression check](../frontend/src/service/__tests__/shipping-reference-service.test.ts). |
| LEGACY-007 | Impossible dates could be accepted or normalized to another date. | Strict parsing; retain invalid text for correction. [UI fix 214c665d](https://github.com/bcgov/nr-lexis/commit/214c665d), [input preservation 41471c7e](https://github.com/bcgov/nr-lexis/commit/41471c7e), [backend fix 905cdb3d](https://github.com/bcgov/nr-lexis/commit/905cdb3d). |
| LEGACY-008 | Invoice validation allowed characters outside the database's US-ASCII storage contract. | Reject unsupported characters before saving. [8d8bff03](https://github.com/bcgov/nr-lexis/commit/8d8bff03). Source/validation finding; no live legacy failure demonstrated. |
| LEGACY-009 | A positive fee override such as `0.001` rounded to zero and became disabled. | Require a positive value after two-decimal rounding. [54e7fb47](https://github.com/bcgov/nr-lexis/commit/54e7fb47). |
| LEGACY-010 | Search joins duplicated exemption/permit rows, counts and balances. | Deduplicate relationships and aggregate volumes separately. [15501690](https://github.com/bcgov/nr-lexis/commit/15501690); [regression protection b67776f0](https://github.com/bcgov/nr-lexis/commit/b67776f0). |
| LEGACY-011 | Legacy submitter saves read staff controls absent from the page. | Build updates from authorized edit state. [27fb49f8](https://github.com/bcgov/nr-lexis/commit/27fb49f8). |

## Proposed procedure changes — deferred pending a business request

| ID | Finding | Proposed procedure correction | Legacy application changes and release scope |
| --- | --- | --- | --- |
| LEGACY-012 | `OT` species volume is omitted from Other subtotals. | Include `OT` in `PERMIT_LEDGER_REPORT`, `SPECIES_GRADE_REPORT_CSV`, `SPECIES_GRADE_RPT` and `SPECIES_GRADE_REGION_SUBRPT`. | **Procedure only** for the subtotal calculation; no new output field is needed. Permit Ledger PDF/CSV can use the existing application paths. Species/Grade PDF: procedure only expected. A complete Species/Grade CSV repair also needs the Java mapping correction described in LEGACY-014. |
| LEGACY-013 | CSV's end-date assignment checks the start-date parameter, breaking one-sided ranges. | In `SPECIES_GRADE_REPORT_CSV`, guard the upper bound with `P_DATE_TO`. | **Procedure only** for the date guard: legacy already passes both optional dates correctly. A complete legacy CSV repair also needs the Java mapping correction in LEGACY-014 for the other criteria. The legacy PDF routines already check the correct date parameter. |
| LEGACY-014 | CSV ignores the selected exemption type. | In `SPECIES_GRADE_REPORT_CSV`, assign the supplied exemption type instead of retaining `%`. | **Legacy application code + procedure** for CSV: correct the seven shifted criteria in `SpeciesGradeReportAction.generateCSV()` to match the existing DAO parameter order, preserving the PDF path. Exemption reason currently reaches exemption type. This is one shared Java fix/application release for the CSV dependencies in LEGACY-012–016. The legacy PDF routines already assign exemption type. |
| LEGACY-015 | All three Species/Grade routines ignore Forest file ID. | Apply the file filter without multiplying scale rows; confirm the matching relationship. | **Legacy application code + procedures** for CSV: apply the same Java mapping correction as LEGACY-014; permit status currently reaches Forest file ID. **Procedure only expected** for legacy PDF, whose Java action already sends the named file criterion. |
| LEGACY-016 | Joining by exemption duplicates scales and can assign volume to the wrong product; one synthetic 10 m³ scale appeared as 10 m³ Harvested plus 10 m³ Standing. | Count each scale once using its owning package/application; confirm ordinary and BOIC classification rules. | **Procedure only** for aggregation, preserving the existing result columns. A complete legacy CSV repair also needs the Java mapping correction in LEGACY-014 so totals use the requested criteria. Legacy PDF: procedure only expected. |
